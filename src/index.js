import ReactDOM from 'react-dom/client';
import App from './App';

// Get the root element from the DOM
ReactDOM.createRoot(
    document.getElementById('root')
    // Render the app
).render(<App />);